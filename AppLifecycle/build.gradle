apply plugin: 'groovy'
apply plugin: 'maven'

dependencies {
    implementation gradleApi()
    implementation localGroovy()
    implementation 'com.android.tools.build:transform-api:1.5.0'
    implementation 'com.android.tools.build:gradle:3.6.0'
}

group='com.github'
version='1.0.0'

//classpath '${group}:${moduleName}:${version}'
//classpath 'com.zr.asm:Simple:1.0.0'

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: uri('../rep'))
            //maven {
            //       url uri('rep')
            //      }
        }
    }
}
